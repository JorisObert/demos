import 'package:flutter/material.dart';

class EmptyPool extends StatelessWidget {
  const EmptyPool({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Nothing to show here :('));
  }
}
